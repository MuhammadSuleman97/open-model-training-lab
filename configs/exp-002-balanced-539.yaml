model: models/cache/models--Qwen--Qwen3-1.7B-MLX-bf16/snapshots/720c04346ea2b095c801ebbd545c109230964cd4
train: true
fine_tune_type: lora
optimizer: adam
data: data/banking77/exp_002_balanced_539
seed: 3408

# One complete pass over 539 balanced training records.
num_layers: 16
batch_size: 1
iters: 539
val_batches: 154
learning_rate: 1.0e-5
steps_per_report: 25
steps_per_eval: 270
grad_accumulation_steps: 1

adapter_path: adapters/exp-002-balanced-539
# Save only at completion so an interrupted run can safely restart from zero.
save_every: 539
test: false
max_seq_length: 576
grad_checkpoint: false
mask_prompt: true
report_to: null

lora_parameters:
  rank: 8
  dropout: 0.0
  scale: 20.0
  keys:
    - self_attn.q_proj
    - self_attn.v_proj
