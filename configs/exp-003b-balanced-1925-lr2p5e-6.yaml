model: models/cache/models--Qwen--Qwen3-1.7B-MLX-bf16/snapshots/720c04346ea2b095c801ebbd545c109230964cd4
train: true
fine_tune_type: lora
optimizer: adam
data: data/banking77/exp_003_balanced_1925
seed: 3408

# Stable retry: one pass over the same 1,925 records, with only the learning
# rate changed from the failed 1e-5 run.
num_layers: 16
batch_size: 1
iters: 1925
val_batches: 154
learning_rate: 2.5e-6
steps_per_report: 50
steps_per_eval: 963
grad_accumulation_steps: 1

adapter_path: adapters/exp-003b-balanced-1925-lr2p5e-6
save_every: 1925
test: false
max_seq_length: 576
grad_checkpoint: false
mask_prompt: true
report_to: null

lora_parameters:
  rank: 8
  dropout: 0.0
  scale: 20.0
  keys:
    - self_attn.q_proj
    - self_attn.v_proj
